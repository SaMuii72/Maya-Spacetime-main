export function getInteractionSnippet(toneName: string, signName: string, toneNumber: number) {
  return `
When the Tone ${toneNumber} (${toneName}) combines with the ${signName} day sign,
this creates a personality that expresses the sign's energy through the tone's life lesson.
This combination shapes how inner purpose becomes visible in the outer world.
  `.trim();
}
